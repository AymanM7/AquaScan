# Loaded by Next.js from frontend/.env.local
# The root .env is NOT read by Next.js (it only reads .env* from its own project root).
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_DEMO_MODE=false
NEXT_PUBLIC_DEMO_REP_ID=rep@grundfos.com
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1...
